# План разработки «Премиального планировщика»

## Общие правила выполнения

- Источник требований — `tz.md`; новые функции вне ТЗ не добавляются.
- Итоговая реализация состоит только из `index.html`, `style.css` и `script.js`.
- Используются чистые HTML, CSS и JavaScript без библиотек, CDN и внешних шрифтов.
- Целевой браузер — актуальный настольный Google Chrome, минимальное окно `1280 × 720`, оптимальное — `1440 × 900` и больше.
- После каждого этапа приложение должно запускаться и предоставлять законченный пользовательский результат, а не набор незавершённых заготовок.
- После каждого изменения сначала выполняются доступные автоматические проверки: синтаксис, консоль, действия в браузере, сохранение и восстановление данных, граничные условия.
- Ручная проверка пользователя ограничивается тем, что нельзя достоверно оценить автоматически: внешний вид, понятность, визуальный комфорт и удобство взаимодействия.
- Найденные автоматическими проверками ошибки исправляются до передачи этапа пользователю.
- Тестовые данные после проверки удаляются или заменяются чистым состоянием.

## Этап 1. Основа приложения и единый интерфейс

**Статус: ✅ выполнен 23 августа 2026 года.**

### Результат этапа

Работающее пустое приложение с приветствием, постоянным каркасом, навигацией, календарными датами, темами, двумя языками и надёжной основой локального хранения. После перезагрузки настройки восстанавливаются.

### Реализация

- Создать три файла реализации и базовую семантическую HTML-структуру.
- Построить единый каркас: сворачиваемая боковая панель, верхняя панель, центральная область, правая панель редактора и слой уведомлений.
- Реализовать приветствие из трёх шагов и пустое состояние после его завершения.
- Реализовать разделы «Обзор», «Неделя», «День», «Идеальная неделя», «Поиск», «Настройки» и «Справка» с переключением без перезагрузки страницы.
- Реализовать выбор даты, предыдущей/следующей недели, кнопку «Сегодня», ISO-недели с понедельника и локализованные диапазоны дат.
- Реализовать выделение сегодняшней и выбранной даты.
- Реализовать русскую и английскую локализацию всех созданных системных элементов.
- Реализовать системную, светлую бело-золотую и тёмную чёрно-золотую темы.
- Заложить внутренние сервисы и контроллеры `script.js` согласно ТЗ, не смешивая хранение, расчёты и отрисовку.
- Реализовать разделённые ключи `localStorage`, версию схемы, базовые настройки, индикатор «Сохранение / Сохранено / Ошибка» и восстановление состояния.
- Реализовать обнаружение смены часового пояса без сдвига сохранённых локальных дат и часов.
- Добавить базовые подтверждения, баннеры ошибок и пустые состояния.

### Автоматическая проверка

- Проверить отсутствие синтаксических ошибок HTML, CSS и JavaScript.
- Открыть приложение в Chrome и убедиться, что в консоли нет ошибок.
- Пройти приветствие и проверить, что повторная загрузка не показывает его снова.
- Проверить переключение всех разделов, сворачивание боковой панели и открытие/закрытие правой панели.
- Проверить переходы через границы месяца, года и ISO-недели.
- Проверить кнопку «Сегодня» и одновременное отображение состояний «сегодня» и «выбрано».
- Переключить RU/EN и все три режима темы, перезагрузить страницу и проверить восстановление выбора.
- Проверить создание только согласованных ключей `premiumPlanner.*` и отсутствие одного монолитного ключа базы.
- Смоделировать повреждённое значение настройки и проверить безопасную обработку без падения страницы.
- Проверить работу при ширине 1280 px и появление предупреждения при меньшей ширине.

### Ручная проверка пользователя

- Оценить, выглядит ли каркас премиально, спокойно и цельно в светлой и тёмной темах.
- Проверить, понятны ли навигация, выбранная дата, кнопка «Сегодня» и три шага приветствия.
- Оценить удобство интерфейса при `1280 × 720` и `1440 × 900`.

### Критерий завершения

Пустой планировщик полностью запускается, навигация и настройки работают, данные настроек сохраняются, а автоматические проверки этапа проходят без ошибок.

### Фактически сделано

- Созданы `index.html`, `style.css` и `script.js`; внешние библиотеки, шрифты, CDN и сетевые интеграции не добавлялись.
- Реализован цельный настольный каркас с раскрываемой боковой навигацией, верхней панелью, недельной лентой дат, центральными представлениями, правой контекстной панелью, подтверждениями и уведомлениями.
- Реализованы семь представлений этапа с законченными информативными пустыми состояниями без неработающих кнопок создания.
- Реализовано приветствие из трёх обязательных шагов с выбором языка, темы и звука.
- Реализованы переходы по ISO-неделям и датам, кнопка «Сегодня», отдельные состояния выбранного и сегодняшнего дня, локализованный заголовок недели.
- Реализованы русский и английский интерфейс, системная, светлая и тёмная темы, мгновенное применение и сохранение настроек.
- Реализованы базовые внутренние сервисы локализации, дат, тем и хранения, а также контроллер приложения; логика отделена от отрисовки.
- Создаются только разделённые ключи `premiumPlanner.meta`, `premiumPlanner.settings`, `premiumPlanner.ui` и `premiumPlanner.weekIndex`; монолитный ключ базы отсутствует.
- Реализованы отложенное сохранение, финальная запись при закрытии, индикатор состояния и безопасный переход к настройкам по умолчанию при повреждённом JSON.
- Реализовано обнаружение смены часового пояса без сдвига локального календаря.
- Реализованы справочные контекстные панели для недели, дня и идеальной недели, подтверждение повторного приветствия и поиск в пустом состоянии.

### Выполненные автоматические проверки

- JavaScript прошёл синтаксическую проверку; после полного набора взаимодействий в браузерной консоли нет ошибок и предупреждений.
- Проверены три шага приветствия, переходы назад/вперёд, применение настроек и отсутствие повторного показа после перезагрузки.
- Проверено переключение всех семи разделов, сворачивание панели из двух мест, открытие/закрытие правой панели и подтверждения, включая клавишу Escape.
- Проверены календарные переходы через границу 2026/2027 года, номер ISO-недели, возврат к текущей дате и совместное состояние «сегодня + выбрано».
- Проверен корректный русский формат диапазона: `Неделя 34 · 17–23 августа 2026`.
- Проверены светлая и тёмная темы, RU/EN, звук и положение боковой панели до и после перезагрузки.
- Проверены размеры `1280 × 720`, `1440 × 900` и предупреждение при ширине 1100 px; горизонтального переполнения на поддерживаемых размерах нет.
- Проверены все 96 статических ключей локализации: пропущенных русских или английских текстов нет.
- В изолированном тесте проверены повреждённые настройки, безопасные значения по умолчанию, четыре разрешённых ключа хранилища, отсутствие `premiumPlanner.data`, ISO-неделя и невалидная дата.
- Проверено отсутствие внешних URL и внешних ресурсов в исходных файлах.

### Дополнения сверх первоначальной формулировки этапа

- Для приветствия, подтверждений и правой панели добавлена блокировка фонового интерфейса через `inert`, чтобы клавиатура не уходила под открытый слой.
- Добавлены полноценные премиальные превью будущих недельных и дневных блоков; они показывают структуру продукта, но не имитируют ещё не реализованное редактирование.
- Пустой поиск реагирует на введённый запрос корректным сообщением, не выдавая фиктивных результатов.
- Никакой новой продуктовой функции сверх `tz.md` не добавлено.

### Корректировки после ручной проверки

- 23 августа 2026 года в разделе «День» количество видимых строк блока «Остальные задачи» приведено к установленному лимиту: отображаются все 10 строк.
- Дневная сетка и сетка «Идеальной недели» расширены до полного диапазона `06:00–24:00`: в каждой по 36 получасовых интервалов, полные часы отделены сплошной линией, середины часов — пунктирной.
- Для сохранения цельного интерфейса полные сетки прокручиваются внутри своих карточек; заголовки колонок остаются видимыми.

## Этап 2. Неделя, цели, привычки и обзорная аналитика

### Результат этапа

Пользователь может полноценно планировать выбранную неделю: вести четыре цели с подзадачами, отмечать три привычки и видеть корректную сводку на обзорной панели.

### Реализация

- Реализовать ровно четыре карточки целей для каждой недели.
- Добавить название, описание, один из восьми цветов, ручной прогресс без подзадач и автоматический прогресс при наличии подзадач.
- Реализовать создание, редактирование, отметку, удаление и изменение порядка подзадач с хэштегами.
- Реализовать перестановку целей и подзадач drag-and-drop и кнопочную альтернативу.
- Реализовать очистку цели с подтверждением, потерей связей у обычных записей и 7-секундной отменой.
- Реализовать ровно три привычки, эффективные версии названий по неделям и три состояния дневной отметки.
- Запретить изменение привычек за будущие даты.
- Реализовать компактную сводку целей и привычек.
- Реализовать обзорные показатели и формулы ТЗ: цели, задачи, привычки, общий прогресс, встречи, занятые и свободные часы.
- Исключать из общего прогресса категории без данных.
- На этом этапе активировать быстрое действие «Цель». Быстрые действия «Задача», «Встреча» и «Запись» добавлять вместе с готовностью соответствующих сущностей на этапах 3 и 4; не показывать неработающие кнопки-заглушки.
- Хранить и обновлять данные отдельно по неделям.

### Автоматическая проверка

- Проверить запрет пятой цели и четвёртой привычки с корректным сообщением.
- Проверить ручной прогресс цели без подзадач и автоматический расчёт после добавления первой подзадачи.
- Проверить расчёты при 0%, частичном и полном выполнении подзадач.
- Проверить перестановку целей и подзадач мышью и кнопками.
- Проверить очистку цели, удаление связей и восстановление через Undo.
- Перезагрузить страницу до и после истечения Undo и проверить установленное в ТЗ поведение.
- Проверить наследование и переименование привычки: прошлые недели сохраняют старое название, выбранная и последующие используют новую версию.
- Проверить цикл `не отмечено → выполнено → не выполнено` и запрет будущих отметок.
- Проверить формулы аналитики на наборах без данных, с одной, двумя и тремя заполненными категориями.
- Перейти между несколькими неделями и проверить независимость данных и перезапись только изменённого недельного ключа.

### Ручная проверка пользователя

- Оценить удобство четырёх карточек целей и таблицы трёх привычек.
- Проверить, понятны ли ручной и автоматический прогресс, цвета и три состояния привычки.
- Оценить, легко ли за несколько секунд понять положение дел по обзорной панели.

### Критерий завершения

Цели, подзадачи, привычки и недельная аналитика работают для любых выбранных недель, сохраняются после перезагрузки и проходят проверки формул и лимитов.

## Этап 3. Дневные задачи, расписание и идеальная неделя

### Результат этапа

Пользователь может полностью спланировать день и постоянный шаблон идеальной недели: вести ограниченные списки задач, размещать их по времени, создавать встречи и гибкие блоки, перемещать и растягивать записи.

### Реализация

- Реализовать для каждого дня два независимых списка: три главные и десять остальных задач.
- Реализовать создание, редактирование, выполнение, удаление, изменение порядка и перенос между списками.
- Реализовать строгие лимиты и спокойные пояснения при превышении.
- Реализовать 7-секундный Undo и синхронное сохранение удаления согласно ТЗ.
- Создать дневную сетку 06:00–24:00 с шагом 30 минут и колонками «Жёсткие встречи» и «Гибкие задачи».
- Реализовать создание блока нажатием на ячейку, стандартную длительность 30 минут и правую панель редактирования.
- Реализовать drag-and-drop по времени, между колонками и днями, а также кнопочные и формовые альтернативы.
- Реализовать связь одной задачи со списком и расписанием без дублирования сущности.
- Реализовать изменение длительности растягиванием и через поля.
- Реализовать валидацию диапазона, шага и запрет перехода через полночь.
- Реализовать отображение пересечений рядом и предупреждение без блокировки сохранения.
- Реализовать копирование, дублирование и удаление записей.
- Реализовать три масштаба сетки и автопрокрутку к текущему времени.
- Реализовать идеальную неделю: семь дней, 36 интервалов, название, описание, время, цвет и категория элемента.
- Реализовать ручное копирование идеальной недели в выбранную календарную неделю с предупреждением о дублях и общей отменой операции.

### Автоматическая проверка

- Проверить запрет четвёртой главной и одиннадцатой обычной задачи.
- Проверить перестановку и перенос задач между списками при свободном и заполненном целевом списке.
- Проверить синхронизацию задачи между списком и сеткой и сохранение задачи после удаления только временного блока.
- Создать записи во всех граничных ячейках, включая 06:00 и 23:30–24:00.
- Проверить ошибки для 05:30, времени после 24:00, неверного шага, нулевой длительности и перехода через полночь.
- Проверить перемещение и растягивание мышью, а затем те же действия через форму и кнопки.
- Создать несколько пересечений и проверить расчёт ширины, предупреждения и отсутствие двойного учёта времени в аналитике.
- Проверить перенос записи на другую дату и между колонками с сохранением длительности.
- Проверить три масштаба до и после перезагрузки.
- Создать элементы идеальной недели во всех днях, проверить пересечения и копирование в пустую и заполненную календарную неделю.
- Проверить общую отмену копирования шаблона и её окончательность после перезагрузки.

### Ручная проверка пользователя

- Оценить читаемость получасовой сетки и удобство трёх масштабов.
- Проверить, естественно ли ощущаются перетаскивание, растягивание и перенос между колонками.
- Оценить различимость жёстких встреч, гибких задач, конфликтов и выполненных задач.
- Проверить, не перегружен ли дневной экран при заполненных списках и расписании.

### Критерий завершения

Выбранный день и идеальная неделя полностью редактируются мышью и альтернативными элементами управления, ограничения и конфликты обрабатываются, а состояние без потерь восстанавливается после перезагрузки.

## Этап 4. Записи, повторения, поиск и дополнительные возможности

### Результат этапа

Все содержательные функции планировщика завершены: универсальные записи, повторяющиеся серии, хэштеги, поиск, связанные файлы, диктовка, напоминания и Telegram-превью работают по правилам ТЗ.

### Реализация

- Завершить универсальный редактор для встреч, гибких блоков, дневных задач и записей.
- Реализовать обязательный заголовок, текст, дату, время, связи с целью и привычкой, хэштеги, связанные файлы и напоминания.
- Не добавлять общие статусы и приоритеты.
- Реализовать ежедневные и еженедельные серии, необязательную дату окончания и вычисление экземпляров без бесконечного хранения.
- Реализовать исключения и выбор области изменения: один экземпляр, этот и последующие, вся серия.
- Реализовать хэштеги, нормализацию регистра, подсказки и переход к поиску по тегу.
- Реализовать глобальный поиск по всем неделям с задержкой 250 мс и фильтрами типа, выполнения, даты и тегов.
- Реализовать раздел «Связанные файлы»: выбор любых файлов, сохранение только десяти уникальных имён и ясное пояснение ограничения.
- Реализовать браузерную диктовку RU/EN/AUTO, явное согласие, офлайн-состояние, обработку ошибок и вставку окончательного текста в курсор.
- Реализовать напоминания за 15 минут и в момент начала только при открытой странице.
- Реализовать группировку одновременных баннеров и единственный звуковой сигнал для группы.
- Реализовать настройки визитки, светлый и тёмный варианты и Telegram-превью без внешней отправки.

### Автоматическая проверка

- Проверить обязательность заголовка, ограничения длины и сохранение всех разрешённых полей.
- Создать ежедневные и еженедельные серии с окончанием и без него; проверить экземпляры на нескольких неделях.
- Проверить изменение и удаление одного экземпляра, будущих экземпляров и всей серии.
- Проверить очистку недели с экземплярами серии и создание исключений вместо удаления всей серии.
- Проверить теги с кириллицей, латиницей, цифрами и `_`, нечувствительность к регистру, завершение пробелом и удаление дублей.
- Проверить поиск по каждому типу, всем фильтрам, прошлым и будущим неделям и переход к найденной записи.
- Проверить лимит десяти имён, одинаковые имена и отсутствие чтения содержимого выбранных файлов.
- Проверить состояния диктовки при неподдерживаемом API, отказе в разрешении, офлайн-режиме и сетевой ошибке с сохранением ручного текста.
- При доступном Web Speech API проверить запуск, остановку и вставку итогового текста; содержимое распознавания оценивается только настолько, насколько его возвращает Chrome.
- Смоделировать одно и несколько одновременных напоминаний, проверить группировку, однократный звук и отсутствие пропущенных уведомлений после перезапуска.
- Проверить обязательные поля и оба оформления визитки.
- Открыть Telegram-превью и убедиться, что оно не вызывает сеть, Telegram или системный шеринг.

### Ручная проверка пользователя

- Оценить удобство правой панели при создании длинной записи.
- Проверить понятность настройки повторений и выбора области изменения серии.
- Оценить читаемость результатов поиска и удобство фильтров.
- Проверить, достаточно ли ясно объяснено, что связанные файлы не сохраняются целиком.
- Оценить Telegram-превью и оба оформления визитки.
- Проверить, не раздражают ли баннеры напоминаний и звуковой сигнал.

### Критерий завершения

Записи и серии корректно создаются и находятся, голос и напоминания безопасно деградируют при недоступности, Telegram ничего не отправляет, а все данные сохраняются в согласованной модели.

## Этап 5. Защита данных, справка и финальная проверка

### Результат этапа

Готовая первая версия продукта: данные можно безопасно экспортировать, импортировать и очищать, заполнение хранилища контролируется, ошибки восстанавливаются, интерфейс доступен и все критерии `tz.md` проверены целиком.

### Реализация

- Реализовать экспорт полного версионированного JSON.
- Реализовать валидируемый импорт с обязательным скачиванием текущей копии, ручным подтверждением её сохранения и откатом при ошибке записи.
- Реализовать очистку выбранной недели, старых недель и всех данных с требуемыми подтверждениями.
- Реализовать контроль безопасного бюджета 4 МиБ, предупреждения при 80% и 95% и обработку `QuotaExceededError`.
- Реализовать экран повреждённого хранилища со скачиванием сырой копии, импортом и подтверждённым сбросом.
- Реализовать миграцию версии схемы и ссылочную целостность.
- Завершить настройки и встроенную справку по всем ограничениям продукта.
- Проверить клавиатурную доступность, фокус, подписи, альтернативы drag-and-drop, контраст и `prefers-reduced-motion`.
- Удалить временные заглушки, недостижимый код, отладочные сообщения и тестовые данные.
- Провести полную сквозную проверку всех критериев приёмки из `tz.md`.

### Автоматическая проверка

- Экспортировать заполненную модель, очистить приложение, импортировать копию и сравнить все сущности и настройки.
- Проверить отказ импорта для неверного JSON, неизвестной версии, неверных типов и неполного файла без изменения текущих данных.
- Смоделировать отменённое или неподтверждённое резервное копирование и проверить блокировку импорта/очистки.
- Смоделировать ошибку записи при импорте и проверить восстановление прежнего состояния.
- Проверить очистку выбранной и старых недель без удаления постоянных настроек, визитки и идеальной недели.
- Проверить ввод фраз `УДАЛИТЬ ВСЕ` и `DELETE ALL`, включая неверный регистр и лишние пробелы.
- Заполнить тестовое хранилище до порогов и проверить предупреждения 80%, 95% и `QuotaExceededError`.
- Повредить один и несколько ключей и проверить, что приложение не перезаписывает их молча и позволяет скачать сырой снимок.
- Проверить ссылочную целостность после удаления целей, привычек, задач, недель и серий.
- Повторить основной пользовательский путь в обеих темах и на обоих языках с контролем ошибок консоли.
- Проверить управление Tab, Enter, Space и Escape, возврат фокуса из модальных окон и альтернативы каждому drag-and-drop действию.
- Проверить отсутствие внешних зависимостей, лишних файлов реализации и неожиданных сетевых запросов.
- Выполнить финальную проверку всех критериев раздела 33 `tz.md` и зафиксировать результат по каждому пункту.

### Ручная проверка пользователя

- Провести итоговую оценку внешнего вида в светлой и тёмной темах на основном рабочем разрешении.
- Проверить, понятен ли полный путь от пустого планировщика до заполненной недели без обращения к справке.
- Оценить удобство ежедневной работы: скорость добавления задачи, встречи, цели, отметки привычки и поиска записи.
- Сообщить только о визуальных или UX-моментах, которые субъективно мешают пользоваться продуктом.

### Критерий завершения

Все обязательные функции `tz.md` реализованы, автоматические проверки пройдены, критических ошибок в консоли и сохранении нет, а пользователю остаётся только подтвердить визуальное качество и удобство интерфейса.

## Итоговая последовательность

1. ✅ Основа приложения и единый интерфейс — выполнено 23 августа 2026 года.
2. Неделя, цели, привычки и обзорная аналитика.
3. Дневные задачи, расписание и идеальная неделя.
4. Записи, повторения, поиск и дополнительные возможности.
5. Защита данных, справка и финальная проверка.

Переход к следующему этапу выполняется только после завершения результата текущего этапа и исправления ошибок, найденных его автоматическими проверками.
